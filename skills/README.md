# Skills

This directory contains agent skills and their supporting reference files.

## Skills

- `structured-dev/` — structured software development workflow for code-changing tasks. Subagent execution uses the generic companion `extensions/task` Pi extension, not a structured-dev-specific extension.
